{
  "name": "@localllm/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@localllm/contracts": "workspace:*",
    "@localllm/inference": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
